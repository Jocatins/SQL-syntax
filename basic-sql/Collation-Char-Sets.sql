-- The available character sets on the server can be listed using the following query

SHOW CHARACTER SET;

-- we can list the collations as follows:

SHOW COLLATION;

-- You can inspect the defaults for your server using the following query:

SHOW VARIABLES LIKE "c%";