-- Say we want to capture only the first names of actors in a table.
-- We can create a temporary table and populate it with only the first name as follows:

CREATE TEMPORARY TABLE ActorNames (FirstName CHAR(20));